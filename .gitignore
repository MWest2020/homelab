# Ansible
*.retry
ansible/inventory/host_vars/*/vault.yml
ansible/inventory/hosts.yml

# Secrets - NEVER commit these
*.pem
*.key
**/secrets/

# Editor
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Python (for Ansible)
__pycache__/
*.py[cod]
venv/
.venv/

# Kubernetes
kubeconfig
*.kubeconfig

# Temporary
tmp/
temp/
.tmp-kubeconfig/
*.tmp
*.bak

# Local notes with IPs
hosts
notes.txt
*.local.md

#environment files
.env
*.env
.env.*

# DNS-01 / certbot credentials (generated op de node uit .env)
cloudflare.ini
**/cloudflare.ini

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
.terraform.lock.hcl
terraform.tfvars
# Terraform-planbestanden zijn wegwerpartefacten (en kunnen waarden bevatten
# die je niet in git wilt). Draai een plan opnieuw in plaats van hem te bewaren.
tfplan
*.tfplan
