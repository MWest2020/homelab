apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openanonymiser
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  finalizers:
    - resources-finalizer.argocd.io
spec:
  project: default
  source:
    repoURL: https://github.com/MWest2020/homelab.git
    targetRevision: main
    path: cluster-config/infra/openanonymiser
  destination:
    server: https://kubernetes.default.svc
    namespace: openanonymiser
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
