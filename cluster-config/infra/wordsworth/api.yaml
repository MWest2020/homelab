# Wordsworth read API (state/metrics/search/hybrid/ask). Light: no in-process ML.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wordsworth-api
  labels: { app: wordsworth, component: api }
spec:
  replicas: 1
  selector:
    matchLabels: { app: wordsworth, component: api }
  template:
    metadata:
      labels: { app: wordsworth, component: api }
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: api
          image: ghcr.io/mwest2020/wordsworth:sha-41da606
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            # gunicorn 26 writes a control socket under $HOME/.gunicorn; the root
            # fs is read-only, so point HOME at the writable /tmp emptyDir.
            - { name: HOME, value: /tmp }
          envFrom:
            - configMapRef: { name: wordsworth-config }
            - secretRef:    { name: wordsworth-db }
            - secretRef:    { name: wordsworth-s3 }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
          startupProbe:
            httpGet: { path: /health, port: 8000 }
            periodSeconds: 5
            failureThreshold: 24
          readinessProbe:
            httpGet: { path: /health, port: 8000 }
            periodSeconds: 15
            timeoutSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 8000 }
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            # /ingest buffers PDF uploads (a ~45MB outlier seen) + pypdf/boto3.
            # gunicorn --max-requests recycles the worker so memory can't grow
            # unbounded over a long run; 4Gi absorbs a single big-doc spike.
            # Nodes have 16Gi.
            requests: { cpu: "100m", memory: "512Mi" }
            limits:   { cpu: "1",    memory: "4Gi" }
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: wordsworth-api
spec:
  selector: { app: wordsworth, component: api }
  ports:
    - { port: 8000, targetPort: 8000 }
  type: ClusterIP
